# Governance Agent 计划制定 SOP — 通用铁律（Guideline）

## 元数据 Metadata

| 字段 Field | 值 Value |
| --- | --- |
| 文档名 Document | Governance Agent 计划制定 SOP 通用铁律 |
| 主题 Subject | 任何 Governance agent 制定任何计划的强制流程 |
| 版本 Version | v0.1（草案，**待 Owner 确认后生效**） |
| 日期 Date | 2026-09-12 |
| 状态 Status | 通用 guideline（跨项目、跨 harness 适用） |
| 效力 Force | **铁律**：违反任一条 → 相关产出无效、一律重做 |
| 存放位置 Location | `.Teamwork-Guideline/` |

---

## 0. 定位与效力

- 本文件是「**法**」，不是案例集：只含抽象规则，**不含任何具体事故描述或举例**。
- 适用对象：任何 harness 中担任 **Governance agent** 的角色，在制定**任何计划**（摸底 / 核实 / 执行 / 设计 / 审计 / spec……）时。
- 三根支柱：
  1. **意图保真**（Intent Fidelity）—— 用户意图在每一次转译中零损耗；
  2. **过程物理化**（SOP + check 纪律）—— 计划制定过程外化为物理文件并逐项核验；
  3. **知识多源**（Multi-source）—— 默认不信任单一来源，含 agent 自身训练知识。
- 与项目级 spec 冲突时，以**更严格者**为准，同时向 Owner 报告冲突。

---

## 1. 第一阶段：意图保真（Intent Fidelity）—— 一切计划的第一步

### 1.1 转译损耗检查

用户原始意图 → Governance agent 理解 → 计划文本，**每一次转译都是损耗风险点**。必须逐级显式检查：

1. **回读原始指令原文**（不凭记忆，逐字重读）。
2. 显式列出四要素：
   - 用户**要求了**什么（含交付形式）；
   - 用户**禁止了**什么；
   - **隐含前提**是什么；
   - 重点 / 核心是什么。
3. **逐条映射**：每一条用户要求必须能映射到计划中的具体条款。出现任何无映射的要求 = 损耗，必须先修复再继续。

### 1.2 重点与核心识别

- 必须显式标注「重点 / 核心」条目。
- **重点核心必须 200% 正确执行。**
- 同时强制声明：**识别重点 ≠ 其他要求可以忽略或不执行——全部要求都必须执行，无一豁免。**

### 1.3 理解输出与确认闭环

1. 向用户输出理解：复述意图 + 重点核心 + 全部要求清单。
2. **用户确认之前，禁止进行任何下一步正式动作**（起草计划正文、写交付文件、检索、指派执行者）。
3. 用户提出修改 → 回到 1.1 重新做损耗检查，直到获得确认。
4. 确认结果必须**物理留痕**：写入计划文档的「意图保真记录」节（模板见 §6.3），含确认时间。

---

## 2. 第二阶段：SOP 建立（过程物理化）

### 2.1 禁止脑内制定

- 生成任何计划之前，必须先产出 **SOP list 物理文件**。
- SOP list 的含义：**「要正确生成这个计划，Governance agent 应该做哪些事」** 的显式清单。
- 不得以「已经想清楚了」替代物理文件；没有物理 SOP，后续一切动作无效。

### 2.2 SOP list 最小结构（位置强制）

```text
S0     Recheck SOP list 自身（见 2.3）—— 固定首项
S1…Sn  计划制定工作项（每项含：目标 / 动作 / 产出物 / check 判据 / 物理凭据要求）
T-4    SOP 纪律合规复查 —— 固定倒数第四项
T-3    组装、生成计划 candidate —— 固定倒数第三项
T-2    audit 计划 candidate —— 固定倒数第二项
T-1    确认 T-2 audit 已真实执行 —— 固定最后一项
```

尾部四项 **不可增删、不可换位**。S1…Sn 由 Governance agent 依据意图保真记录设计。

### 2.3 S0：SOP 自检（SOP 的第一步是检查 SOP 自身）

SOP 建立后的第一个动作 = **检查 SOP list 设计是否合理**：

| 检查维度 | 判据 |
| --- | --- |
| 覆盖性 | 意图保真记录中的全部要求逐条映射到 SOP 工作项，无遗漏 |
| 顺序 | 工作项依赖关系成立，无逻辑倒置 |
| 可执行性 | 每项有明确动作与物理产出物 |
| 判据明确 | 每项的 check 判据可物理判定（非「感觉完成」） |
| 结构合法 | S0 首项 + 尾部四项位置正确 |

S0 不通过 → 修订 SOP → 重新 S0。S0 通过后 SOP **冻结**；后续修订须记录理由并重新自检。

---

## 3. 执行与 check 纪律（铁律）

### 3.1 逐项 check 制度

执行 SOP 的唯一合法方式：

```text
执行一个工作项 → 回到 SOP 物理文件对照 check 该项 → 判定通过 + 凭据入台账 → 才能进入下一项
```

check 结果记入 check 台账（模板见 §6.2），**每条 check 必须附物理凭据**。

### 3.2 五严禁

1. **严禁提前 check**：工作项未真实执行完，禁止先记录 check。
2. **严禁补 check**：跳过 check 后回头补记 = 违规；被跳过的项必须重做。
3. **严禁跳过 check**：任何工作项不允许无 check 直接通过。
4. **严禁无凭据 check**：check 必须基于真实、正确、**物理可查**的凭据；禁止以执行动作的记忆、工具返回的「成功」字样替代物理复核。
5. **不确定禁止 check**：存疑 = 未通过；禁止在不确定状态下打勾。

### 3.3 判定纪律

- 判定值必须使用 SOP 预定义的**判定字典**，禁止自创、混用、合并判定值。
- 判定必须与该项**自身的**凭据一一对应；禁止以其他项的凭据覆盖本项目。

### 3.4 违反后果

- 任何违反 check 纪律的工作项及其**下游全部产出** → 一律视为无效，必须重做。
- 重做未完成 → **不得交付**。
- 违规本身必须如实记录；重做不冲销违规记录。

---

## 4. 多源知识组装铁律（强制，非可选）

### 4.1 默认不信任单一来源

单一来源 = 不可信。**单一来源包括 agent 自身训练库中的默认答案**——训练知识只可作为检索线索，不得直接作为事实采信。

### 4.2 非常识性内容的强制多源与交叉比对

凡涉及**非常识性**的设计决策、事实断言、技术行为声明：

1. 必须**多源检索**（≥2 个相互独立的来源）。
2. 必须**交叉比对**：
   - 一致 → 采信，显式标注**全部**来源；
   - 不一致 → 显式列出差异与冲突点，交 Owner 裁定或降级为「待验证项」；**禁止 silently 择一采信**。

### 4.3 常识豁免必须显式

判定某内容为「常识」（允许单源）时，必须显式声明并说明依据；**默认从严**（拿不准 = 不是常识）。

### 4.4 无法多源时的处理

显式标注「单源 / 未交叉验证」+ 降低置信级别 + 在计划中列为待验证项；**禁止当作已验证事实使用**。

---

## 5. 固定尾部四项（位置强制，不可增删换位）

### 5.1 T-4：SOP 纪律合规复查

检查**以上全部已执行工作项**：每次 check 是否遵守了本 SOP 的 check 纪律（§3.2 / §3.3）。

- 发现任何违规（提前 / 补 / 跳 / 无凭据 / 不确定即 check）→ **该次执行一律视为无效**，回到违规点重做，重做后重新走到本项。
- 本项自身同样需要物理凭据（逐项复核台账记录）。

### 5.2 T-3：组装、生成计划 candidate

把 S1…Sn 的全部产出组装为完整计划 candidate，必须包含：

- 意图保真记录（含用户确认留痕）；
- SOP list 与 check 台账（全量）；
- 多源记录（§6.4）；
- 目录与产出位置规划（交付物 / 证据 / 运行时临时，三类区）。

### 5.3 T-2：audit 计划 candidate

对 candidate 做审计，**至少**覆盖以下维度，每维度独立给结论：

| 维度 | 判据 |
| --- | --- |
| 意图保真回溯 | 计划条款与已确认意图逐条一致，重点核心 200% 落实 |
| 角色边界纯度 | 凡要求执行者引用 / 复述 / 逐字搬运的字段，只含该执行角色职责范围内的内容，**不得混入其他角色的判断性内容** |
| 要求覆盖完整性 | 用户全部要求无一遗漏（重点核心 + 其余全部） |
| 多源合规 | 全部非常识性内容有多源 + 交叉比对记录 |
| 目录规划完整 | 交付物 / 证据 / 运行时三类区齐备且位置明确 |
| 判定字典一致 | 计划中所有判定值有定义、无混用 |

### 5.4 T-1：确认 audit 已真实执行

用**独立于 T-2 执行动作本身的物理手段**复核：audit 产物存在、审计维度逐一有结论、凭据可查。**不是凭记忆确认。**

---

## 6. 模板（物理文件骨架，随计划一并交付）

### 6.1 SOP list 模板

```markdown
# SOP list — <计划名>
| ID | 工作项 | 目标 | 动作 | 产出物 | check 判据 | 物理凭据要求 | check 结果 | 凭据链接 | check 时间 |
|---|---|---|---|---|---|---|---|---|---|
| S0 | SOP 自检 | … | … | … | §2.3 五维度 | … | | | |
| S1 | … | | | | | | | | |
| …  | … | | | | | | | | |
| T-4 | SOP 纪律合规复查 | | | | §5.1 | | | | |
| T-3 | 组装计划 candidate | | | | §5.2 | | | | |
| T-2 | audit candidate | | | | §5.3 六维度 | | | | |
| T-1 | 确认 audit 已执行 | | | | §5.4 | | | | |
```

### 6.2 check 台账模板

```markdown
| ID | check 时间(UTC) | 判定 | 物理凭据（路径/内容锚点） | 备注 |
|---|---|---|---|---|
```

### 6.3 意图保真记录模板

```markdown
| 字段 | 内容 |
|---|---|
| 原始指令原文 | （逐字粘贴） |
| 复述理解 | |
| 重点 / 核心 | |
| 全部要求清单 | （逐条编号） |
| 用户确认结果与时间 | |
```

### 6.4 多源记录模板

```markdown
| 断言 ID | 断言内容 | 来源 1 | 来源 2 | 交叉结果（一致/不一致/单源） | 置信级别 | 备注 |
|---|---|---|---|---|---|---|
```

---

## 7. 交付门槛

计划可交付 = 以下**全部**为真：

1. 意图保真记录完整且经用户**物理留痕确认**；
2. SOP list 物理文件存在，S0 通过并冻结；
3. check 台账完整：每项有判定 + 物理凭据，五严禁零违规；
4. T-4 / T-3 / T-2 / T-1 依序完成且有各自凭据；
5. 多源记录覆盖全部非常识性内容。

任一不满足 → **不得交付**。

---

## 8. 生效与修订

- 本文件 v0.1 为草案，**经 Owner 确认后生效**。
- 修订须记录版本、理由与日期；修订后，新启动的计划即时适用新版本。
- 本文件对本项目全部 Governance agent（任何 harness）具有铁律效力，且**任何时候适用**。

— 文件结束 —
