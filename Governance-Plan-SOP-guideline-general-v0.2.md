# Governance Agent 计划制定 SOP — 通用铁律（Guideline）

## 元数据 Metadata

| 字段 Field | 值 Value |
| --- | --- |
| 文档名 Document | Governance Agent 计划制定 SOP 通用铁律 |
| 主题 Subject | 任何 Governance agent 制定任何计划的强制流程 |
| 版本 Version | v0.2 |
| 日期 Date | 2026-09-12 |
| 状态 Status | 通用 guideline（跨项目、跨 harness 适用） |
| 效力 Force | **铁律**：违反任一条 → 相关产出无效，必须按规则重做 |
| 存放位置 Location | `.Teamwork-Guideline/` |

---

## 0. 定位与效力

- 本文件是「**法**」，不是案例集：只含抽象规则，不依赖具体事故或案例。
- 适用对象：任何 harness 中担任 **Governance agent** 的角色，在制定任何计划时。
- 三根支柱：
  1. **意图保真**（Intent Fidelity）—— 用户要求不得被静默遗漏、改写或弱化；
  2. **过程物理化**（SOP + check 纪律）—— 计划制定过程必须形成可检查的物理记录；
  3. **知识多源**（Multi-source）—— 非常识性事实、技术行为和设计依据不得无验证采信。
- 与项目级 spec 冲突时，以**更严格者**为准，同时向 Owner 报告冲突。
- 本文件中的“必须 / 不得 / 禁止”均为强制要求。

---

## 1. 第一阶段：意图保真（Intent Fidelity）

### 1.1 原始指令与要求提取

任何计划开始前：

1. **回读用户原始指令原文**，不得仅凭记忆。
2. 显式提取：
   - 用户**要求了**什么（含交付形式）；
   - 用户**禁止了**什么；
   - 已知的**前提 / 约束**是什么；
   - 哪些是**重点 / 核心要求**。
3. 建立“要求 → 计划条款”的逐条映射。
4. 任何无法映射的用户要求，必须先修复，不得继续生成计划。

### 1.2 重点 / 核心要求

- 必须显式标记重点 / 核心要求。
- 重点 / 核心要求必须有**额外独立复核**。
- 重点被识别后，不得因此忽略其他要求；**全部要求都必须执行**。

### 1.3 理解与确认闭环

1. 向用户输出：
   - 理解；
   - 重点 / 核心；
   - 全部要求清单；
   - 必要的约束与假设。
2. **用户确认之前，不得进入正式计划制定、正式交付文件编写、正式执行指派。**
3. 用户修改需求 → 重新执行 §1.1。
4. 用户确认结果必须留痕，写入「意图保真记录」。
5. 若用户未确认，不得自行视为确认。

> 注：为理解原始需求而进行的必要语义核实，不属于正式执行；不得借此提前执行用户尚未确认的任务。

---

## 2. 第二阶段：SOP 建立（过程物理化）

### 2.1 禁止脑内制定

- 生成计划之前，必须先产出 **SOP list 物理记录**。
- SOP list 必须明确“为了正确生成这个计划，需要做哪些工作”。
- 不得以“已经想清楚了”“我知道怎么做了”替代 SOP。

### 2.2 SOP list 最小结构

```text
S0     Recheck SOP list 自身（见 2.3）—— 固定首项
S1…Sn  计划制定工作项
T-4    SOP 纪律合规复查 —— 固定倒数第四项
T-3    组装、生成计划 candidate —— 固定倒数第三项
T-2    audit 计划 candidate —— 固定倒数第二项
T-1    确认 T-2 audit 已真实执行 —— 固定最后一项
```

尾部四项**不可增删、不可换位**。

S1…Sn 由 Governance agent 根据已确认的意图、约束和必要证据设计。

### 2.3 S0：SOP 自检

SOP 建立后的第一个动作 = 检查 SOP 自身：

| 检查维度 | 判据 |
| --- | --- |
| 覆盖性 | 已确认要求全部映射，无遗漏 |
| 顺序 | 依赖关系成立，无明显逻辑倒置 |
| 可执行性 | 每项都有明确动作和实际产出 |
| 可检查性 | 每项都有可以实际判定的 check 判据 |
| 结构合法 | S0 首项 + 固定尾部四项位置正确 |
| 能力可行性 | 所需工具、权限、文件或环境在当前 Harness 中可获得；否则必须明确标记无法执行 |

S0 不通过 → 修订 SOP → 重新 S0。

S0 通过后 SOP **冻结**。

若用户要求发生变化、环境发生实质变化，或已发现 SOP 本身存在错误：

- 原 SOP 不得直接覆盖；
- 标记旧版本失效；
- 形成新版本；
- 新版本必须重新执行 S0。

---

## 3. 执行与 check 纪律

### 3.1 逐项 check

唯一合法流程：

```text
执行一个工作项
→
依据该项自己的 check 判据进行 check
→
记录判定与物理凭据
→
通过后才能进入下一项
```

不得把多个工作项全部执行完再统一补 check。

### 3.2 Check 判定

工作项 check 只使用：

```text
PASS
FAIL
```

- **PASS**：该项判据全部满足，且有有效物理凭据。
- **FAIL**：该项判据未满足，或凭据不足以证明满足。

无法确定是否满足 → **FAIL**，不得打 PASS。

### 3.3 有效物理凭据

有效凭据必须能够被重新检查，而不是仅依赖 Agent 的记忆或一句“成功”。

可作为凭据的包括：

- 实际文件及其内容；
- 实际命令 / 测试输出；
- 实际运行结果；
- 可重新访问的源材料；
- 可定位的日志、diff、记录或链接；
- 经独立复核得到的实际结果。

以下**单独存在时不构成充分凭据**：

- Agent 自己声称“已完成”；
- 工具返回一句“success”；
- Agent 的记忆或解释；
- 没有可定位对象的口头结论。

### 3.4 五严禁

1. **严禁提前 check**：工作项尚未真实完成，不得 check。
2. **严禁补 check**：跳过 check 后再补记，视为违规；相关工作项必须重做。
3. **严禁跳过 check**：任何工作项不得无 check 直接通过。
4. **严禁无凭据 check**：没有有效物理凭据，不得 PASS。
5. **不确定禁止 PASS**：存疑 = FAIL。

### 3.5 凭据必须对应自身工作项

- 每条 check 必须有对应凭据。
- 不得使用其他工作项的凭据代替本项凭据。
- 一个凭据可以支持多个工作项，但必须明确说明它支持哪些项；不得默认推定。

### 3.6 违规后果

任何违反 §3 check 纪律的工作项：

- 该项无效；
- 依赖该项结果的下游产出无效；
- 必须从违规点重新执行；
- 原违规记录必须保留；
- 重做成功不得删除或冲销原违规记录。

---

## 4. 多源知识组装铁律

### 4.1 默认不直接信任单一来源

Agent 自身训练知识只能作为线索，不得直接作为未经验证的事实依据。

但并非所有内容都需要外部多源检索。

### 4.2 内容分类

需要判断的信息至少区分：

| 类型 | 默认要求 |
| --- | --- |
| 用户明确要求 / 约束 | 以用户原始记录为依据 |
| 项目现有文件 / 本地实际状态 | 优先以实际文件、运行结果等直接证据为依据 |
| 事实 / 技术行为声明 | 非常识性内容需要验证 |
| Agent 推论 | 明确标记为推论，不得伪装成事实 |
| 方案决策 | 给出决策依据，不得伪装成外部事实 |
| 假设 | 必须显式标记，不得静默转化为事实 |

### 4.3 非常识性内容的验证

凡涉及非常识性的：

- 事实断言；
- 技术行为；
- 外部规则；
- 关键设计依据；

原则上需要：

**≥2 个真正独立的依据，或 1 个可靠来源 + 1 个独立实际验证。**

“独立”指依据不能只是互相转载同一内容。

若不同来源发生冲突：

1. 显式记录差异；
2. 不得静默择一；
3. 能由实际验证解决时，应优先验证；
4. 不能解决时，交 Owner 裁定或降级为待验证项。

### 4.4 无法完成多源验证

如果客观条件导致无法多源：

- 必须明确标记“单源 / 未交叉验证”；
- 不得冒充已完成交叉验证；
- 对该内容降低置信度；
- 必要时列入待验证项。

---

## 5. 固定尾部四项

## 5.1 T-4：SOP 纪律合规复查

检查前序全部已执行工作项：

- check 是否真实发生；
- 是否存在提前 check；
- 是否存在补 check；
- 是否存在跳 check；
- 是否存在无凭据 PASS；
- 是否存在不确定却 PASS；
- 判定是否均使用 `PASS / FAIL`；
- 凭据是否能够实际复查。

发现违规：

```text
发现违规
→
违规点及其依赖下游失效
→
回到违规点重做
→
重新执行后续流程
→
再次执行 T-4
```

T-4 自身也必须有物理凭据。

---

## 5.2 T-3：组装、生成计划 candidate

把前序产出组装为完整计划 candidate。

candidate 至少包含：

- 意图保真记录；
- SOP list；
- check 台账；
- 必要的多源 / 验证记录；
- 产出位置规划；
- 必要的执行、验证和交付要求。

candidate 必须明确版本。

不得在 T-3 后直接把 candidate 当成最终有效计划。

---

## 5.3 T-2：audit 计划 candidate

对 candidate 进行独立审计，至少覆盖：

| 维度 | 判据 |
| --- | --- |
| 意图保真 | 用户已确认的要求全部落实 |
| 重点 / 核心 | 每项均已落实并完成额外独立复核 |
| 要求完整性 | 非重点要求同样无遗漏 |
| 角色边界 | 不把其他角色的判断、结论或未授权内容混入执行者职责 |
| 多源合规 | 需要验证的内容具有相应依据 |
| Check 合规 | 判定、凭据和台账符合本 Guideline |
| 可执行性 | 当前 Harness 能够实际执行计划所要求的动作 |

每个维度必须有独立结论和对应凭据。

### T-2 失败处理

T-2 出现任何未通过项：

```text
T-2 FAIL
→
修复 candidate
→
生成新 candidate 版本
→
重新 T-2
```

不得把 FAIL 的 candidate 直接交付。

---

## 5.4 T-1：确认 audit 已真实执行

T-1 不是重复说一句“已经 audit 过”。

必须通过独立复查确认：

- T-2 audit 记录真实存在；
- 所有规定的审计维度都有结论；
- 审计结论有可查凭据；
- 当前 candidate 版本与被审计版本一致；
- T-2 的结论不是仅由 T-2 自己的口头声明证明。

T-1 应至少重新读取或重新验证关键 audit 产物；**不得仅复述 T-2 自己的结论作为证明。**

---

## 6. 模板

### 6.1 SOP list 模板

```markdown
# SOP list — <计划名>

| ID | 工作项 | 目标 | 动作 | 产出物 | check 判据 | 物理凭据要求 | check 结果 | 凭据链接 | check 时间 |
|---|---|---|---|---|---|---|---|---|---|
| S0 | SOP 自检 | … | … | … | §2.3 | … | | | |
| S1 | … | … | … | … | … | … | | | |
| … | … | … | … | … | … | … | | | |
| T-4 | SOP 纪律合规复查 | … | … | … | §5.1 | … | | | |
| T-3 | 组装计划 candidate | … | … | … | §5.2 | … | | | |
| T-2 | audit candidate | … | … | … | §5.3 | … | | | |
| T-1 | 确认 audit 已执行 | … | … | … | §5.4 | … | | | |
```

### 6.2 Check 台账模板

```markdown
| ID | check 时间(UTC) | 判定 | 物理凭据（路径/内容锚点） | 备注 |
|---|---|---|---|---|
```

判定只能使用：

```text
PASS
FAIL
```

### 6.3 意图保真记录模板

```markdown
| 字段 | 内容 |
|---|---|
| 原始指令原文 | （逐字保留） |
| 复述理解 | |
| 重点 / 核心 | |
| 用户要求清单 | （逐条编号） |
| 禁止事项 | |
| 前提 / 约束 | |
| 用户确认结果 | |
| 用户确认时间 | |
```

### 6.4 多源 / 验证记录模板

```markdown
| 断言 ID | 内容 | 类型 | 依据 1 | 依据 2 / 独立验证 | 结果 | 备注 |
|---|---|---|---|---|---|---|
```

`类型`至少可使用：

```text
FACT
OWNER_CONSTRAINT
OBSERVATION
INFERENCE
DECISION
ASSUMPTION
```

---

## 7. 交付门槛

计划可交付 = 以下条件全部满足：

1. 意图保真记录完整，且用户确认有物理留痕；
2. SOP list 存在，S0 已 PASS；
3. 所有已执行工作项都有 check；
4. 每个 PASS 都有有效物理凭据；
5. 五严禁零违规，或所有违规已按本 Guideline 完成重做；
6. T-4 / T-3 / T-2 / T-1 依序完成；
7. T-2 为通过状态；
8. 所需的事实 / 技术行为 / 外部规则均已按 §4 完成必要验证；
9. 当前 candidate 版本与最终审计版本一致；
10. 最终状态汇报必须使用统一状态集合：

```text
STATUS=SUCCESS
STATUS=PARTIAL
STATUS=FAILED_ROLLED_BACK
STATUS=FAILED_TRANSIENT
STATUS=FAILED_SYNTAX
STATUS=FAILED_UNRECOVERABLE
STATUS=NEEDS_CLARIFICATION
STATUS=NEEDS_AUTHORIZATION
```

计划最终可正常交付时，使用：

```text
STATUS=SUCCESS
```

其他状态必须如实反映当前实际情况，不得用 `SUCCESS` 掩盖：

- 未完成；
- 无法验证；
- 需要用户澄清；
- 需要授权；
- 临时故障；
- 无法恢复的失败。

### 7.1 异常状态处理

不得自行创造新的最终 STATUS。

遇到异常时，根据实际情况使用已有状态：

| 状态 | 使用场景 |
|---|---|
| `PARTIAL` | 部分完成，但仍有明确未完成项 |
| `FAILED_ROLLED_BACK` | 执行失败，且已回滚到规定安全状态 |
| `FAILED_TRANSIENT` | 临时性故障，理论上可重试 |
| `FAILED_SYNTAX` | 因语法、格式或解析错误导致失败 |
| `FAILED_UNRECOVERABLE` | 当前条件下无法恢复或继续 |
| `NEEDS_CLARIFICATION` | 必须获得用户澄清才能继续 |
| `NEEDS_AUTHORIZATION` | 必须获得授权才能继续 |

异常状态下：

- 不得伪造 PASS；
- 不得为了得到 `SUCCESS` 而省略失败记录；
- 不得把“无法验证”自动解释为“已通过”。

---

## 8. 生效与修订

- 本文件 v0.2 在 Owner 确认后生效。
- 新启动的计划使用当前生效版本。
- 已经启动的计划如遇本 Guideline 修订：
  - 若新规则影响正确性或安全性，应按新规则处理；
  - 若不影响当前计划，保持原版本记录不变。
- 修订必须记录：
  - 版本；
  - 日期；
  - 修改原因。
- 不得直接覆盖历史版本而导致历史执行记录失去可追溯性。

---

## 9. 核心禁令摘要

以下规则不得因任务规模、时间压力、Agent 自身判断或工具便利而绕过：

```text
1. 不得静默丢失用户要求。
2. 不得在用户未确认前正式执行未经确认的计划。
3. 不得无物理 SOP 直接制定计划。
4. 不得跳过逐项 check。
5. 不得提前 check 或事后补 check。
6. 不得无有效凭据 PASS。
7. 不得把不确定当作 PASS。
8. 不得以 Agent 自身记忆代替事实验证。
9. 不得静默处理来源冲突。
10. 不得把失败、未验证或未完成伪装成 SUCCESS。
11. 不得删除历史违规记录。
12. 不得让 FAIL 的 candidate 直接交付。
```

— 文件结束 —